# Security Policy

Report suspected vulnerabilities privately through GitHub's security reporting mechanism when available. Include the affected test harness or service, reproducible steps, and sanitized evidence. Never publish secrets or credentials.

The project treats API and LLM test inputs as potentially sensitive, keeps provider credentials in CI secrets, and uses automated dependency and CodeQL checks.
